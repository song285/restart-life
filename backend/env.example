# 后端环境变量配置示例
# 复制此文件为 .env 并修改相应配置

# 服务器端口
PORT=3001

# CORS 允许的来源（多个用逗号分隔）
# 如果前端通过 Nginx 代理访问，可以留空或设置为前端地址
# 例如: CORS_ORIGIN=http://43.163.83.15,http://your-domain.com
# 注意：如果使用 Nginx 代理，前端使用相对路径 /api，则不需要配置此项
CORS_ORIGIN=http://localhost:3000,http://localhost:5173,http://43.163.83.15

# 数据库路径（可选，默认使用相对路径）
# DATABASE_PATH=/var/www/restart-life/data/database.db

# Gemini API Key（用于生成每日问候）
GEMINI_API_KEY=your_gemini_api_key_here

# 邮件服务配置（SMTP）
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_email_password

# Twilio 短信服务配置（可选）
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=your_twilio_phone_number

# 运行环境
NODE_ENV=production
