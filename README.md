# 重启人生 - 智能家居安全签到应用

<div align="center">
<img width="1200" height="475" alt="GHBanner" src="https://github.com/user-attachments/assets/0aa67016-6eaf-458a-adb2-6e31a0763ed6" />
</div>

一款智能家居安全签到和紧急警报应用程序，旨在保障个人安全和日常健康监测。

## ✨ 功能特性

- 📅 **每日打卡签到** - 记录每日安全状态
- 📊 **数据统计** - 可视化打卡记录和连续打卡天数
- ⚙️ **个性化设置** - 邮件/短信通知、自动报警配置
- 📞 **紧急联系人** - 管理紧急联系人（最多5个）
- 🤖 **AI问候** - 每日AI生成的个性化问候语

## 🚀 快速开始

### 前置要求
- Node.js 18+ 
- npm 或 yarn

### 方式一：使用启动脚本（推荐）

```bash
# 给脚本添加执行权限（仅首次）
chmod +x start.sh

# 启动前后端服务
./start.sh
```

### 方式二：手动启动

#### 1. 启动后端服务

```bash
cd backend
npm install
npm run dev
```

后端服务将在 http://localhost:3001 启动

#### 2. 启动前端应用

```bash
# 在项目根目录
npm install
npm run dev
```

前端应用将在 http://localhost:5173 启动

### 环境变量配置

#### 后端配置 (backend/.env)
```env
PORT=3001
DATABASE_PATH=./data/database.db
GEMINI_API_KEY=your_gemini_api_key_here  # 可选，用于AI问候
CORS_ORIGIN=http://localhost:5173
```

#### 前端配置 (.env)
```env
VITE_API_URL=http://localhost:3001/api
```

## 📁 项目结构

```
重启人生/
├── backend/              # 后端API服务
│   ├── src/
│   │   ├── db/         # 数据库配置
│   │   ├── models/     # 数据模型
│   │   ├── routes/     # API路由
│   │   └── services/   # 业务服务
│   └── data/           # 数据库文件
├── components/          # React组件
├── services/            # 前端API服务
└── ...
```

## 🗄️ 数据库

使用 SQLite 数据库，首次启动会自动创建以下表：
- `users` - 用户表
- `checkins` - 打卡记录表
- `user_settings` - 用户设置表
- `emergency_contacts` - 紧急联系人表

详细数据库设计请查看 [数据库设计.md](./数据库设计.md)

## 📡 API接口

详细API文档请查看 [backend/README.md](./backend/README.md)

主要接口：
- `POST /api/checkin` - 创建打卡
- `GET /api/checkin/stats` - 获取统计
- `GET /api/settings` - 获取设置
- `PUT /api/settings` - 更新设置
- `GET /api/contacts` - 获取联系人
- `POST /api/contacts` - 创建联系人
- `DELETE /api/contacts/:id` - 删除联系人
- `GET /api/greeting` - 获取每日问候

## 🛠️ 技术栈

### 前端
- React 19
- TypeScript
- Vite
- React Router
- Recharts
- Tailwind CSS

### 后端
- Node.js
- Express
- TypeScript
- SQLite (better-sqlite3)
- Google Gemini AI (可选)

## 📝 开发说明

- 数据库会在首次启动时自动创建
- 所有数据存储在 `backend/data/database.db`
- 后端已配置CORS，允许前端访问
- Gemini API Key是可选的，不配置也能正常运行

## 📚 更多文档

- [项目说明.md](./项目说明.md) - 完整项目说明
- [数据库设计.md](./数据库设计.md) - 数据库设计文档
- [backend/README.md](./backend/README.md) - 后端API文档

## 📄 License

MIT
